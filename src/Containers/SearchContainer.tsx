import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const SearchContainer = () => {
  return (
    <View>
      <Text>This is Search Container</Text>
    </View>
  )
}

export default SearchContainer

const styles = StyleSheet.create({})
